# AI Provider Abstraction

Providers implement `generate`, optional `embed`, and `health_check`. The MVP includes mock, Ollama-compatible, OpenAI-compatible, and 0G Compute scaffold providers. Credentials are environment variables only.
